coverage:
  precision: 2
  round: down
  status:
    project:
      default:
        target: 84%
        threshold: 0%
        if_ci_failed: error
        if_not_found: failure
    patch:
      default:
        target: 90%
        threshold: 0%
        if_ci_failed: error
        if_not_found: failure
