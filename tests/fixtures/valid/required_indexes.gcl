// Required index declarations — bindable via parameterized import.
dim Velocity = Length / Time;
dim Acceleration = Length / Time^2;

// Required named index (no variants specified)
pub(bind) index Phase;

// Required range indexes with dim constraints
pub(bind) index Step: Time;
pub(bind) index Accel: Length / Time^2;
