// main.gcl -- rebinds the lib's Widget type to MyWidget
type MyWidget {
    MyWidget(size: Length),
}

include lib.lib(type Widget: MyWidget, value: MyWidget(size: 1.0 m))::{ origin };

node result: Length = @origin.size;
