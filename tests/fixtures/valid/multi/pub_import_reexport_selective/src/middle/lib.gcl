// middle.gcl -- selective re-export of `GravityAccel` (issue #452).
//
// `pub GravityAccel` inside the brace list marks the imported dim for
// re-export at middle.gcl's visible surface, so a downstream importer
// can reach it transitively.
import middle.leaf.{ pub GravityAccel, g0 };
