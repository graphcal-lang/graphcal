// A local `const unit` definition can reference a selectively imported unit (#822).
import app.units.{ mile };

const unit halfmile: Length = 0.5 mile;

param a: Length = 1609.344 m;
node b: Length = @a -> halfmile;
