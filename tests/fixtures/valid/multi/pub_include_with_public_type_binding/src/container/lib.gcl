// container.gcl -- exposes a bindable type through a parametric value body.
pub(bind) type Element {
    Element(size: Length),
}

pub type Wrapper<T: Type = Element> {
    Wrapper(value: T),
}

param value: Element;
pub node origin: Element = @value;
