// container.gcl -- same library as the V006 negative fixture.
pub(bind) type Element {
    Element(size: Length),
}

pub type Wrapper<T: Type = Element> {
    Wrapper(value: T),
}

pub const node origin: Element = Element(size: 1.0 m);
