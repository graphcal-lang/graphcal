// main.gcl -- selectively re-exports `origin` with Element bound to a `pub`
// local type → compiles. Positive companion to the V006 fixture.
pub type Inner {
    Inner(size: Length),
}

include container.lib(Element: Inner).{ pub origin, pub Wrapper };
