// main.gcl -- selective include; does NOT import the assertion explicitly
// The assertion in lib.gcl should still be evaluated and reported.
include lib().{ doubled };

node result: Dimensionless = @doubled + 1.0;
