// middle.gcl -- selective re-export of `Acceleration` (issue #452).
//
// `pub Acceleration` inside the brace list marks the imported dim for
// re-export at middle.gcl's visible surface, so a downstream importer
// can reach it transitively.
import middle.leaf.{ pub Acceleration, g0 };
