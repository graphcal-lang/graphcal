// Mix of bare module path and file-based import/include
import nasa.constants.{ g0 };
import local_params.{ isp };

dim Velocity = Length / Time;
dim Acceleration = Length / Time^2;

node v_exhaust: Velocity = @isp * @g0;
