// main.gcl -- tries to bind a named index to a range (should fail)
base dimension Time;
index TimeStep = linspace(0.0, 1.0, step: 0.5);

#[allow_defaults]
import "./lib.gcl"(
    Phase: TimeStep,
    cost: for t: TimeStep { 1.0 },
) { cost };
