// Required index declarations — must be bound via parameterized import.
// This file cannot be evaluated standalone.
dimension Velocity = Length / Time;
dimension Acceleration = Length / Time^2;

// Required named index (no variants specified)
cat Phase;

// Required range indexes with dimension constraints
range Step: Time;
range Accel: Length / Time^2;
