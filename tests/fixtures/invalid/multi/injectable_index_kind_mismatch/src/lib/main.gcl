// main.gcl -- tries to bind a named index to a coordinate index (should fail)
index TimeStep = range(0.0, 1.0, step: 0.5);

include lib.lib(index Phase: TimeStep, cost: for t: TimeStep { 1.0 })::{ cost };
