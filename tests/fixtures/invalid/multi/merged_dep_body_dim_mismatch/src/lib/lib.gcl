// lib.gcl -- invalid reusable template under Option A: the executable body
// relies on the concrete default of bindable `Speed`.
pub(bind) dim Speed = Length / Time;

pub const node v: Speed = 10.0 m/s;
