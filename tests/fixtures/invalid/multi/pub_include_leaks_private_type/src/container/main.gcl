// main.gcl -- selectively re-exports `origin` with Element bound to a
// private-local type → V006 (A9 case 2).
type PrivateInner {
    PrivateInner(size: Length),
}

include container.lib(type Element: PrivateInner)::{ pub origin };
