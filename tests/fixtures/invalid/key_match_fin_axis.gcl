// Only named-axis keys support exhaustive label matching; a Fin-axis key
// has positions, not labels.
index Maneuver = { Departure };

param k: Key<Fin(3)>;

node x: Dimensionless = match @k {
    Maneuver#Departure => 1.0,
};
