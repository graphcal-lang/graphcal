// Error: field access on multi-variant type (should use match instead)
pub dim Velocity = Length / Time;

pub type ManeuverKind {
    Impulsive(delta_v: Velocity),
    Coasting,
}

param m: ManeuverKind = Coasting;

node v: Velocity = @m.delta_v;
