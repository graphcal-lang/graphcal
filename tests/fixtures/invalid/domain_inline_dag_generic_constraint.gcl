// Inline DAG type definitions follow the same generic-argument constraint policy.
dag nested {
    type Wrapper<T: Type> {
        Wrapper(value: T),
    }
    type Bad {
        Bad(value: Wrapper<Length(min: 0.0 m)>),
    }
}
